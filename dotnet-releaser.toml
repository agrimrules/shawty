[msbuild]
project = "shawty/shawty.csproj"

[msbuild.properties]
PublishSingleFile = "true"
SelfContained = "true"

[github]
user = "agrimrules"
repo = "shawty"
version_prefix = "v"
branches = ["main"]
publish = true

[nuget]
publish = false

[pack]
# Enable standard platform packages (zip/tarball)
# The tool will automatically create these for common targets.

[brew]
# This will automatically create and update a Homebrew tap repository.
# By default, it will look for a repository named 'homebrew-tap' under your user.
# You can customize the name if you prefer a different repository.
# publish = true
# name = "shawty"
# tap = "agrimrules/homebrew-tap"
