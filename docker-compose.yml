services:
  shawty:
    build:
      context: .
      dockerfile: shawty/Dockerfile
    ports:
      - "5277:8080"
    volumes:
      - ./sqlite:/sqlite
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - OpenAI__ApiKey=${OPENAI_API_KEY}
